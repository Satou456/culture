spring:
  application:
    name: chinese-national-culture-museum
  web:
    resources:
      add-mappings: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/cultural_museum
    username: root
    password: 023579
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  # Redis 配置
  data:
    redis:
      host: localhost
      port: 6379
      password: 023579
      database: 0
      timeout: 2000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
          max-wait: -1ms

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      table-underline: true
#      id-type: assign_id
  type-handlers-package: com.baomidou.mybatisplus.extension.handlers, satou.community.handler
  mapper-locations: classpath:mapper/*.xml

mybatis:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

# Sa-Token 配置
sa-token:
  # token 名称（同时也是 cookie 名称）
  token-name: Authorization
  # token 有效期，单位秒，-1 代表永不过期
  timeout: 2592000
  # 是否允许同一账号并发登录（为 true 时允许一起登录，为 false 时新登录挤掉旧登录）
  is-concurrent: true
  # 在多人登录同一账号时，是否共用一个 token（为 true 时所有登录共用一个 token，为 false 时每次登录新建一个 token）
  is-share: false
  # token 风格（uuid | simple-uuid | random-32 | random-64 | random-128 | tik）
  token-style: uuid
  # 是否输出操作日志
  is-log: true
  # 是否从 header 读取 token
  is-read-header: true
  # 是否从 cookie 读取 token
  is-read-cookie: false


aliyun:
  oss:
    endpoint: Https://oss-cn-beijing.aliyuncs.com
    bucketName: teststorage1
    region: cn-beijing

