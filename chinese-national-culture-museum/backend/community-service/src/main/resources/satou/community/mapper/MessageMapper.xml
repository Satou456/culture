<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="satou.community.mapper.MessageMapper">

    <!-- 获取两人之间的会话记录 -->
    <select id="getConversation" resultType="satou.community.domain.entity.Message">
        SELECT *
        FROM message
        WHERE (sender_id = #{userId} AND receiver_id = #{friendId})
           OR (sender_id = #{friendId} AND receiver_id = #{userId})
        ORDER BY create_time
        LIMIT #{offset}, #{limit}
    </select>

    <!-- 获取最近联系人相关的消息（每个联系人只取最新一条） -->
    <!-- 逻辑：查找以当前用户为接收者或发送者的所有消息中，每个对手方的最新一条 -->
    <select id="getRecentContacts" resultType="satou.community.domain.entity.Message">
        SELECT m.*
        FROM message m
                 INNER JOIN (SELECT IF(sender_id = #{userId}, receiver_id, sender_id)
                                                     as partner_id,
                                    MAX(create_time) as max_time
                             FROM message
                             WHERE sender_id = #{userId}
                                OR receiver_id = #{userId}
                             GROUP BY partner_id
                             ORDER BY max_time DESC
                             LIMIT #{limit}) t ON m.create_time = t.max_time
            AND ((m.sender_id = #{userId} AND m.receiver_id = t.partner_id)
                OR (m.receiver_id = #{userId} AND m.sender_id = t.partner_id))
        ORDER BY m.create_time DESC
    </select>

    <!-- 获取未读消息数 -->
    <select id="getUnreadCount" resultType="java.lang.Long">
        SELECT COUNT(*) FROM message
        WHERE receiver_id = #{userId}
          AND sender_id = #{senderId}
          AND create_time &gt; (SELECT MAX(create_time) FROM message WHERE sender_id = #{userId} AND receiver_id = #{senderId})
        -- 简单逻辑：统计所有发给我的且我还没回复过的？
        -- 更通用的未读逻辑通常需要有一个 read_status 字段。
        -- 鉴于表结构没有 read_status，这里采用简单策略：
        -- 统计所有 receiver_id = 我 的消息总数 - 我已发送给对方消息后之后的消息(不准确)
        -- **修正**：由于表结构限制，通常私信系统需要 read_status。
        -- 如果必须基于现有表结构，我们暂时只实现“获取所有发给我的消息”，前端根据本地缓存判断未读。
        -- 或者我们假设：只要是我收到的，且时间在最后一次我回复之后，就算未读。
        -- 下面的SQL是实现“最后一次交互后的新消息”
    </select>

    <!-- 重新实现一个简单的未读计数：统计所有发给我，且我没有作为发送者回应过该特定会话的消息（不严谨） -->
    <!-- 最佳实践建议添加 read_status 字段。此处为了适配现有表，暂不实现复杂未读逻辑，仅提供基础查询 -->
</mapper>