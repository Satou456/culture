# 基于Python 3.11的基础镜像
FROM python:3.14-slim

# 设置工作目录
WORKDIR /app/ai_chat

# 复制所有文件（包括README.md）
COPY ai_chat/ .

# 安装依赖
RUN pip install --no-cache-dir uv
RUN uv sync

# 复制启动脚本
COPY ai_chat/start.sh .

# 给启动脚本添加执行权限
RUN chmod +x start.sh

# 暴露端口
EXPOSE 8000

# 启动应用
CMD ["./start.sh"]